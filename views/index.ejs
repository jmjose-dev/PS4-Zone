<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>Playstation 4 Database</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    </head>
    <body>
        <div class="container is-fluid">
            <h1>Playstation 4 Database</h1>
            <div class="columns">
                <%for(i=0;i<parsedBody.length;i++) { %>
                    
                    <div class="column">
                        <b><%=parsedBody[i].name %></b><br>
                        <img src='<%=parsedBody[i].image.super_url%>'>
                    </div>
                <% } %>
            </div>
        </div>
    </body>
</html>